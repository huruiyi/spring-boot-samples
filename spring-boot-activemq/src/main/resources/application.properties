# ActiveMQ Broker URL (use embedded broker if external not available)
spring.activemq.broker-url=tcp://localhost:61616
# Enable in-memory broker as fallback
spring.activemq.in-memory=true
# Trust all packages for object deserialization
spring.activemq.packages.trust-all=true

# Server
server.port=8080